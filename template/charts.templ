<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, shrink-to-fit=no, initial-scale=1">
    <meta name="Markets prediction and analytics" content="">
    <meta name="Eric" content="">

    <title>ixm - the intelligent exchange monitor</title>

    <!-- Bootstrap Core CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href="css/simple-sidebar.css" rel="stylesheet">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
        <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->

</head>

<body>

    <div id="wrapper">

        <!-- Sidebar -->
        <div id="sidebar-wrapper">
            <ul class="sidebar-nav">
                <li class="sidebar-brand">
                    <a href="/">
                        ixm
                    </a>
                </li>
                <li>
                    <a href="/info">Info</a>
                </li>
                <li>
                    <a href="/charts">Charts</a>
                </li>
                <li>
                    <a href="/docs-api">API Docs</a>
                </li>
                <li>
                    <a href="/about">About</a>
                </li>
                <li>
                    <a href="/stats">Stats</a>
                </li>
            </ul>
        </div>
        <!-- /#sidebar-wrapper -->

        <!-- Page Content -->
        <div id="page-content-wrapper">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">
                        <h1>Charts</h1>
                        <p>This page queries the ixm API internally and constructs charts based on current data. Think of it as a live client for the api ticker.</p>
                        <a href="#menu-toggle" class="btn btn-default" id="menu-toggle">Toggle Menu</a>
						<hr>

						<p>Select market:</p>

						<p>
						<div class="dropdown">
						<button class="btn btn-default dropdown-toggle" type="button" id="menu1" data-toggle="dropdown">Market
					    	<span class="caret"></span></button>
					    	<ul class="dropdown-menu" role="menu" aria-labelledby="menu1">
								{{range .Markets}}
									<li role="presentation"><a role="menuitem" tabindex="-1" href="?market={{.}}">{{.}}</a></li>
								{{end}}
					    	</ul>
						</div>
						</p>
						<hr>

						<p>
						<strong>Market:</strong> <code>{{ .Market }}</code>
						<br>
						<strong>Timestamp:</strong> <code>{{ .Timestamp }}</code>

						<table class="table table-striped">
  							<tr>
    							<th>Time</th>
    							<th>Value</th> 
    							<th>MAPE</th>
								<th>Delta</th>
							</tr>
							{{range $index, $value := .Steps}}
							<tr>
								<td>+{{ $value }} hr</td>
								<td>{{ index $.Inferences $index }}</td>
								<td>{{ index $.Metrics $index }}</td>
								<td>{{ index $.Deltas $index }}</td>
							</tr>
							{{end}}
						</table>	
						</p>
                    </div>
                </div>
            </div>
        </div>
        <!-- /#page-content-wrapper -->

    </div>
    <!-- /#wrapper -->

    <!-- jQuery -->
    <script src="js/jquery.js"></script>

    <!-- Bootstrap Core JavaScript -->
    <script src="js/bootstrap.min.js"></script>

    <!-- Menu Toggle Script -->
    <script>
    $("#menu-toggle").click(function(e) {
        e.preventDefault();
        $("#wrapper").toggleClass("toggled");
    });
    </script>

</body>

</html>
